let knex;_0d2‍.x([["getCameraById",()=>getCameraById]]);_0d2‍.w("../database",[["default",["knex"],function(v){knex=v}]]);// 1. Define higher order functions
// 2. Create base query
// 3. Create query builders using higher-order functions
// 4. Apply query builders to the base query



// Define function
const filterById = id => {
    return (query) => query.where('id', id);
};

// Create base query
const cameraBaseQuery = knex('cameras');

// Create and apply query builders using function to the base query
       const getCameraById = async id =>
    filterById(await cameraBaseQuery.where({ 'cameras.id' : id }))[0];


