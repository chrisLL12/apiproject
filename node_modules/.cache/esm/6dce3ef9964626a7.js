let getCameraById;_964‍.x([["default",()=>_964‍.o]]);_964‍.w("../services/cameras",[["getCameraById",["getCameraById"],function(v){getCameraById=v}]]);

const wrapSubmitter = camera => {
    camera.submitter = { id: camera.submitter_id };
    delete camera.submitter_id;
    return camera;
};

const resolvers = {
    //addDrink
    //getDrink
    camera: async ({ id }) => wrapSubmitter(await getCameraById(id))
};

_964‍.d(resolvers);